# Performance profile: tuned caching for a smoother UI.
# Start with: npm run dev:performance

VITE_APP_PROFILE=performance
VITE_USE_REAL_API=true

# Optional: set a full backend base URL (otherwise uses /api + Vite proxy)
VITE_API_BASE_URL=

VITE_API_PROXY_TARGET=http://localhost:15002
